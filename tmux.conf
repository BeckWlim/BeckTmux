# Beck tmux — a quiet, Monokai-aligned workspace
#
# This is intentionally self-contained: it works with a stock tmux install and
# does not require oh-my-tmux or TPM.  If oh-my-tmux is installed later, keep
# this file as the final override (source it after the upstream config).

# -- terminal and interaction -----------------------------------------------
set -g default-terminal "tmux-256color"
set -as terminal-features ",*:RGB"
# Advertise the same true-colour capability to applications inside tmux.
# Neovim uses COLORTERM to enable its RGB highlight path; without it, custom
# GUI-only highlights (such as the dashboard's green sections) degrade to the
# pane default foreground.
set-environment -g COLORTERM "truecolor"
set -g focus-events on
set -g escape-time 0
set -g history-limit 100000
set -g display-time 1800
set -g repeat-time 600
set -g renumber-windows on
set -g base-index 1
set -g pane-base-index 1
set -g set-clipboard on
set -g mouse on

# Vi is the shared editing language with Neovim.  C-a is the sole tmux prefix;
# C-q remains available to Neovim's UI-layer close mappings, and C-Space
# remains available to shells, terminal applications, and input methods.
set -g status-keys vi
set -g mode-keys vi

# tmux's built-in vi-edit table gives command prompts the same two-state flow
# as Neovim: input starts in insert mode, Escape switches to navigation mode
# (including the arrow keys), and i returns to insert mode.  Keep this native
# table intact so the behavior also survives an oh-my-tmux base configuration.

# -- Monokai palette ---------------------------------------------------------
# These remain tmux's default night colours. Applications may opt in with
# scripts/theme.sh to publish @beck_palette_* on their own pane, then reset on
# exit/suspend. A tmux-owned watcher also clears overrides when their publisher
# exits or loses the foreground terminal, even if application cleanup fails.
# Formats resolve the pane's values without replacing defaults.
set -g @beck_default_bg "#272822"
set -g @beck_default_surface "#2f302a"
set -g @beck_default_cursor "#3a3d3f"
set -g @beck_default_border "#666666"
set -g @beck_default_fg "#f8f8f2"
set -g @beck_default_muted "#a6a69c"
set -g @beck_default_green "#a6e22e"
set -g @beck_default_cyan "#66d9ef"
set -g @beck_default_yellow "#e6db74"
set -g @beck_default_purple "#ae81ff"
set -g @beck_default_red "#f92672"

# Applications publish only @beck_palette_* on their pane. Resolve local
# colours separately from chrome: W:/P: select this session's current
# window and active pane even while an inactive window tag is rendered.
set -g @beck_pane_bg "#{?@beck_palette_bg,#{@beck_palette_bg},#{@beck_default_bg}}"
set -g @beck_pane_surface "#{?@beck_palette_surface,#{@beck_palette_surface},#{@beck_default_surface}}"
set -g @beck_pane_cursor "#{?@beck_palette_cursor,#{@beck_palette_cursor},#{@beck_default_cursor}}"
set -g @beck_pane_border "#{?@beck_palette_border,#{@beck_palette_border},#{@beck_default_border}}"
set -g @beck_pane_fg "#{?@beck_palette_fg,#{@beck_palette_fg},#{@beck_default_fg}}"
set -g @beck_pane_muted "#{?@beck_palette_muted,#{@beck_palette_muted},#{@beck_default_muted}}"
set -g @beck_pane_green "#{?@beck_palette_green,#{@beck_palette_green},#{@beck_default_green}}"
set -g @beck_pane_cyan "#{?@beck_palette_cyan,#{@beck_palette_cyan},#{@beck_default_cyan}}"
set -g @beck_pane_yellow "#{?@beck_palette_yellow,#{@beck_palette_yellow},#{@beck_default_yellow}}"
set -g @beck_pane_purple "#{?@beck_palette_purple,#{@beck_palette_purple},#{@beck_default_purple}}"
set -g @beck_pane_red "#{?@beck_palette_red,#{@beck_palette_red},#{@beck_default_red}}"
set -g @beck_pane_window_active_fg "#{?@beck_palette_window_active_fg,#{@beck_palette_window_active_fg},#{E:@beck_pane_green}}"
set -g @beck_pane_window_active_bg "#{?@beck_palette_window_active_bg,#{@beck_palette_window_active_bg},#{E:@beck_pane_cursor}}"
set -g @beck_pane_window_inactive_fg "#{?@beck_palette_window_inactive_fg,#{@beck_palette_window_inactive_fg},#{E:@beck_pane_muted}}"
set -g @beck_pane_window_inactive_bg "#{?@beck_palette_window_inactive_bg,#{@beck_palette_window_inactive_bg},#{E:@beck_pane_bg}}"
set -g @beck_ui_bg "#{W:,#{P:,#{E:@beck_pane_bg}}}"
set -g @beck_ui_surface "#{W:,#{P:,#{E:@beck_pane_surface}}}"
set -g @beck_ui_cursor "#{W:,#{P:,#{E:@beck_pane_cursor}}}"
set -g @beck_ui_border "#{W:,#{P:,#{E:@beck_pane_border}}}"
set -g @beck_ui_fg "#{W:,#{P:,#{E:@beck_pane_fg}}}"
set -g @beck_ui_muted "#{W:,#{P:,#{E:@beck_pane_muted}}}"
set -g @beck_ui_green "#{W:,#{P:,#{E:@beck_pane_green}}}"
set -g @beck_ui_cyan "#{W:,#{P:,#{E:@beck_pane_cyan}}}"
set -g @beck_ui_yellow "#{W:,#{P:,#{E:@beck_pane_yellow}}}"
set -g @beck_ui_purple "#{W:,#{P:,#{E:@beck_pane_purple}}}"
set -g @beck_ui_red "#{W:,#{P:,#{E:@beck_pane_red}}}"
set -g @beck_ui_window_active_fg "#{W:,#{P:,#{E:@beck_pane_window_active_fg}}}"
set -g @beck_ui_window_active_bg "#{W:,#{P:,#{E:@beck_pane_window_active_bg}}}"
set -g @beck_ui_window_inactive_fg "#{W:,#{P:,#{E:@beck_pane_window_inactive_fg}}}"
set -g @beck_ui_window_inactive_bg "#{W:,#{P:,#{E:@beck_pane_window_inactive_bg}}}"

# One neutral base plane, with semantic colours reserved for focus/state.
set -g pane-border-style "fg=#{E:@beck_ui_border},bg=#{E:@beck_ui_bg}"
set -g pane-active-border-style "fg=#{E:@beck_ui_border},bg=#{E:@beck_ui_bg}"
set -g pane-border-lines single
set -g pane-border-status top
set -g pane-border-format "#[fg=#{E:@beck_ui_muted},bg=#{E:@beck_ui_bg},nobold,noreverse]  #{?pane_active,#[fg=#{E:@beck_ui_green}]◈ P#{pane_index}#[fg=#{E:@beck_ui_border}],○ P#{pane_index}}  #{pane_current_command}  "
set -g window-style "fg=#{E:@beck_pane_fg},bg=#{E:@beck_pane_bg}"
set -g window-active-style "fg=#{E:@beck_pane_fg},bg=#{E:@beck_pane_bg}"
# Command prompts use the same planes as Neovim: CursorLine while inserting,
# then the editor background with a green command-mode cue after Escape.
set -g message-style "fg=#{E:@beck_ui_fg},bg=#{E:@beck_ui_cursor},bold"
set -g message-command-style "fg=#{E:@beck_ui_green},bg=#{E:@beck_ui_bg},bold"
# tmux 3.7 draws messages over the status line instead of replacing it.  Keep
# the full-width background used by earlier versions after the server upgrade.
%if "#{m/r:^(3\\.[7-9]|[4-9]\\.),#{version}}"
set -g message-style "fg=#{E:@beck_ui_fg},bg=#{E:@beck_ui_cursor},bold,fill=#{E:@beck_ui_cursor}"
set -g message-command-style "fg=#{E:@beck_ui_green},bg=#{E:@beck_ui_bg},bold,fill=#{E:@beck_ui_bg}"
%endif
# Match Neovim's CursorLine plane for selected scrollback text.
set -g mode-style "fg=#{E:@beck_pane_fg},bg=#{E:@beck_pane_cursor},bold"

# -- statusline --------------------------------------------------------------
set -g status-position bottom
set -g status-justify left
set -g status-interval 1
set -g status-left-length 52
set -g status-right-length 80
set -g status-style "fg=#{E:@beck_ui_muted},bg=#{E:@beck_ui_bg}"
set -g status-left "#{?selection_present,[ VISUAL ],#{?#{m/r:(copy|view)-mode,#{pane_mode}},[ COMMAND ],[ INPUT ]}} #[fg=#{E:@beck_ui_green},bold]󰆍 #S"
# `client_flags` contains `focused` while the terminal displaying this client
# has focus. The tag disappears when focus moves to another terminal window.
set -g status-right "#{?#{m/r:focused,#{client_flags}},#[fg=#{E:@beck_ui_green}#,bold][ FOCUSED ] ,}#[fg=#{E:@beck_ui_muted},nobold]󰉋 #{b:pane_current_path} #[fg=#{E:@beck_ui_border}]│ #[fg=#{E:@beck_ui_cyan}]%H:%M #[fg=#{E:@beck_ui_border}]│ #[fg=#{E:@beck_ui_fg}]%Y-%m-%d #{?client_prefix,│ ⟐ ,}"
set -g window-status-format "#[fg=#{E:@beck_ui_window_inactive_fg},bg=#{E:@beck_ui_window_inactive_bg},nobold,noreverse] #I  #W "
set -g window-status-current-format "#[fg=#{E:@beck_ui_window_active_fg},bg=#{E:@beck_ui_window_active_bg},bold,noreverse] #I  #W "
set -g window-status-separator "  "
set -g window-status-style "fg=#{E:@beck_ui_window_inactive_fg},bg=#{E:@beck_ui_window_inactive_bg},nobold,noreverse"
set -g window-status-current-style "fg=#{E:@beck_ui_window_active_fg},bg=#{E:@beck_ui_window_active_bg},bold,noreverse"

# `refresh-client` does not expand a format in its target, so use `run-shell -C`
# to resolve the event's client name before refreshing that client's footer.
# The one-second status interval above is a fallback for missed terminal events.
set-hook -g client-focus-in 'run-shell -C "refresh-client -S -t #{q/a:hook_client}"'
set-hook -g client-focus-out 'run-shell -C "refresh-client -S -t #{q/a:hook_client}"'

# -- key grammar -------------------------------------------------------------
# Prefix + C-a is the tmux command layer.  Within it, the first letter is a
# stable family: w=workspace, r=resize, o/p=back-and-forward.
unbind -n C-Space
# Remove previous prefix bindings so reloading an attached server releases
# C-b/C-q to applications immediately.
unbind -n C-q
unbind C-q
unbind -n C-b
unbind C-b
set -g prefix C-a
set -gu prefix2
bind C-a send-prefix

# Keep the most useful oh-my-tmux-style ergonomics, expressed as one-key roles.
# r opens a one-key resize table; R remains the explicit reload command.
bind r switch-client -T resize
bind R source-file ~/.config/tmux/tmux.conf \; display-message "tmux reloaded"
# a cycles focus through the split panes; the border tag marks the active one.
bind a select-pane -t :.+
# Keep directional focus changes one-shot.  tmux's stock arrow bindings are
# repeatable, which leaves the prefix table armed for `repeat-time` after the
# pane changes and makes the next application keystroke feel delayed.
bind Up select-pane -U
bind Down select-pane -D
bind Left select-pane -L
bind Right select-pane -R
bind d detach-client
bind ? list-keys

# Windows and panes: mirrors Neovim's <Space>w… family.
bind c new-window -c "#{pane_current_path}"
bind n next-window
bind p previous-window
bind x kill-pane
bind & kill-window
bind w choose-tree -Zw
bind h split-window -h -c "#{pane_current_path}"
bind v split-window -v -c "#{pane_current_path}"
bind o last-window

# Modal terminal layer.  Escape enters scrollback command mode on an ordinary
# shell screen.  Other programs (Neovim, less, fzf, and similar clients) receive
# Escape unchanged and keep ownership of their modes.
bind -n Escape if-shell -F "#{m/r:(^|/)(bash|zsh|fish|sh|dash)$,#{pane_current_command}}" "copy-mode" "send-keys Escape"

# Resize sequences: C-a r j=left, i=up, k=down, l=right.
# The table is one-shot, so the next key returns to the normal prefix table.
bind -T resize i resize-pane -U 3
bind -T resize k resize-pane -D 3
bind -T resize j resize-pane -L 5
bind -T resize l resize-pane -R 5
# Keep the original single-prefix bindings as a repeatable fallback.
bind -r H resize-pane -L 5
bind -r J resize-pane -D 3
bind -r K resize-pane -U 3
bind -r L resize-pane -R 5
bind = select-layout even-horizontal
bind + select-layout even-vertical

# Command mode: vi motions move a free cursor through terminal history.  v enters
# visual selection, y copies and returns to command mode at the same cursor
# position, and i returns to live terminal input.  Lowercase
# j is a convenient, unambiguous prefix key for starting copy mode; uppercase J
# remains the repeatable downward pane resize.
unbind [
bind j copy-mode
bind ] paste-buffer
bind -T copy-mode-vi i send-keys -X cancel
bind -T copy-mode-vi v send-keys -X begin-selection
%if "#{m/r:^(3\\.[6-9]|[4-9]\\.),#{version}}"
# WSL terminal frontends can stall while processing OSC 52 clipboard writes.
# When the native Windows clipboard bridge is available, use it directly.
# -C keeps this copy out of OSC 52 while still adding it to tmux's paste
# buffers.  Test the capability rather than WT_SESSION because that variable
# is not preserved by every terminal or long-running tmux server.
if-shell 'command -v clip.exe >/dev/null 2>&1' {
bind -T copy-mode-vi y send-keys -X copy-pipe -C "clip.exe"
bind -T copy-mode-vi C-c send-keys -X copy-pipe -C "clip.exe"
bind -T copy-mode-vi Enter send-keys -X copy-pipe -C "clip.exe"
} {
bind -T copy-mode-vi y send-keys -X copy-selection
# Keep Ctrl-C consistent with y while browsing scrollback: copy and clear the
# visual selection without leaving command mode or moving the history cursor.
bind -T copy-mode-vi C-c send-keys -X copy-selection
# Enter follows the same non-exiting behavior.  With no active selection,
# copy-selection is a no-op and command mode remains active.
bind -T copy-mode-vi Enter send-keys -X copy-selection
}
%else
bind -T copy-mode-vi y send-keys -X copy-selection
bind -T copy-mode-vi C-c send-keys -X copy-selection
bind -T copy-mode-vi Enter send-keys -X copy-selection
%endif
# Finalize a mouse selection without copying it.  This avoids a redundant
# clipboard update before y/C-c and keeps the selection available in visual
# mode until it is copied or explicitly cancelled with i/q.
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X stop-selection
bind -T copy-mode-vi C-q send-keys -X cancel
bind -T copy-mode-vi q send-keys -X cancel

# Keep the original command visible instead of replacing it with descriptive
# hints such as “(goto line)” or “(search down)”.
# tmux 3.7 adds -e (Backspace exits an empty prompt), while tmux 3.6 added -l
# (use the prompt literally, without inserting a separating space).  Keep the
# 3.4 forms available while the existing server finishes its current sessions.
%if "#{m/r:^(3\\.[7-9]|[4-9]\\.),#{version}}"
bind -T copy-mode-vi : command-prompt -el -p ":" { send-keys -X goto-line "%%" }
bind -T copy-mode-vi / command-prompt -el -T search -p "/" { send-keys -X search-forward "%%" }
%else
bind -T copy-mode-vi : command-prompt -p ":" { send-keys -X goto-line "%%" }
bind -T copy-mode-vi / command-prompt -T search -p "/" { send-keys -X search-forward "%%" }
%endif
bind -T copy-mode-vi ? command-prompt -T search -p "?" { send-keys -X search-backward "%%" }
bind -T copy-mode-vi f command-prompt -1 -p "f" { send-keys -X jump-forward "%%" }
bind -T copy-mode-vi t command-prompt -1 -p "t" { send-keys -X jump-to-forward "%%" }
bind -T copy-mode-vi F command-prompt -1 -p "F" { send-keys -X jump-backward "%%" }
bind -T copy-mode-vi T command-prompt -1 -p "T" { send-keys -X jump-to-backward "%%" }

# Keep terminals readable and reduce visual noise.
set -g allow-passthrough on
set -g status-left-style "fg=#{E:@beck_ui_muted},bg=#{E:@beck_ui_bg}"
set -g status-right-style "fg=#{E:@beck_ui_muted},bg=#{E:@beck_ui_bg}"
set -g pane-active-border-style "fg=#{E:@beck_ui_border},bg=#{E:@beck_ui_bg}"

# Optional oh-my-tmux handoff: if an upstream config is sourced before this
# file, these settings remain the final, local policy.
